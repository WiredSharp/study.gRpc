# [gRpc](https://grpc.io) sandbox

you can find three projects:
- Core
- Client
- Server

Core project build shared base class from included .proto files. Client and Server are respectively client and server based on these definition.